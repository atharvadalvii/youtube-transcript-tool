/**
 * When `true`, Supabase auth gates are skipped so the app (e.g. transcript flow)
 * works without sign-in. Auth routes and server actions stay in place.
 *
 * Set to `false` to require sign-in again.
 */
export const AUTH_DISABLED = true;
