/** Shared layout + typography — keep in sync with the marketing landing page. */
export const SITE_SHELL = "max-w-5xl mx-auto px-5 sm:px-8";

export const siteFontStyle = {
  fontFamily:
    "-apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', system-ui, sans-serif",
} as const;

export const sitePageClassName =
  "min-h-screen bg-white dark:bg-zinc-950 text-gray-900 dark:text-zinc-50 antialiased transition-colors";
